# A:
# 1.bandwidth與bps[相關題]
```
1.Bandwidth頻寬
2.bits per second每秒位元速
3.Bandwidth單位bps
```
# 2.RFID
```
1.Radio Frequency IDentification(無線射頻辨識)
2.無線電識別並讀寫數據
3.靠近或經過就可讀取
```
# 3.NFC
```
1.Near-field communication(近距離無線通訊)
2.商務:NFC手機一卡通
3.支付:Google Pay、Apple Pay
```
# 4.cellular network 
```
1.蜂巢式網路
2.行動通訊硬體架構
3.主流：GSM、3G、4G
```
# 5.technology addiction
```
1.技術成癮
2.無法停止使用
3.忽略作息
```
# 6.Crowdsourcing vs Crowdfunding[比較題]
```
1.群眾外包 vs 群眾募資
2.群眾外包:從群眾取得需要的想法
3.群眾募資:從群眾取得資金完成想法
```
# 7.metadata
```
1.源資料
2.描述屬性
3.利於搜尋
```
# 8.CMS
```
1.content management system(內容管理系統)
2.版本控制
3.協同作業
```
# 9.DDos Attack
```
1.分散式阻斷服務攻擊（distributed denial-of-service attack，簡稱DDoS攻擊）
2.使資源耗盡，無法存取
3.針對重要服務/網站進行攻擊
```
# 10.Botnet
```
1.殭屍網路
2.傳送垃圾封包，使目標癱瘓並阻斷服務
3.可用蠕蟲病毒組成
```
# 11.RWD 
```
1.Responsive web design(回應式網頁設計)
2.網頁設計技術
3.能對應不同平台解析度
```
# 12.SEO 
```
1.search engine optimization(搜尋引擎最佳化)
2.了解搜尋引擎，調整網站
3.提高能見度
```
# B:
# 1.解釋Communications protocol(簡稱協定protocol)?
```
電腦互相通訊的標準
```
# 2.Why Layering?(為何要分層?)
```
改變某層時,不影響他層
```
# 3.列出OSI Model 與 TCP/IP protocol對應圖
 註:1:需用中英文寫出各層的名層
 註:2:須說明OSI Model 每一層的簡略功能
``` 
OSI Model:
7	Application Layer	應用層       應用服務
6	Presentation Layer	表現層       編碼...等
5	Session Layer	        會談層       對談
4	Transport Layer	        傳輸層       End-to-End Connection(端點對端點連線)
3	Network Layer	        網路層       選路徑傳Packed
2	Data Link Layer	        資料連結層   給frame MAC address
1	Physical Layer	        實體層       定義物理與電氣特性

TCP/IP protocol:
4	application layer	應用層	
3	transport layer  	傳輸層
2	internetwork layer	網際網路層	
1	Network Access layer	網路存取層
```
# 4.簡述下列協定的功能與特色並說明他們運作在TCP/IP的哪一層?
(1)HTTP vs HTTPS (2)TELNET vs SSH (3)DNS (4)IP (5)ICMP
```
(1)HTTP(超文本傳輸協定)HyperText Transfer Protocol    vs    HTTPS(超文本傳輸安全協定)HyperText Transfer Protocol Secure
   L7: HTTP是網路通訊基礎，而HTTPS有加密更安全
 
(2)Telnet(遠端登入)    vs    SSH(安全外殼協定)Secure Shell
   L7: Telnet用於Server遠端控制，明文易遭竊聽，而SSH有加密更安全

(3)DNS(網域名稱系統)Domain Name System
   L7: 將域名和IP address對應，易於辨識
 
(4)IP(網際網路協定)Internet Protocol
   L3: Packed交換的Protocol
  
(5)ICMP(網際網路控制訊息協定)Internet Control Message Protocol
   L3: 於IP傳送控制訊息,用於返回錯誤資訊或分析路由
```
# 5.TCP vs UPD
[1]英文全名
[2]須說明relizble(可靠) vs un-reliable(不可靠)
[3]如何達到reliable(可靠)
[4]Three-way handshaking機制
```
[1]TCP(傳輸控制協定)   Transmission Control Protocol
   UDP(用戶資料報協定) User Datagram Protocol
[2]
TCP的可靠
  有三次訊息交換
UDP的不可靠
  會丟包
[3]
(A)錯誤重傳
(B)Three-Way Handshake
[4]
Three-way Handshake (三向交握),三次訊息交換
```
# 6.簡述下列網路設備主要功能與特色及運作在OSI哪一層
(1)Hub vs Repeater (2)Switch vs Bridge (3)Router vs L3 Switch (4)Proxy
```
(1)Hub(集線器)    vs    Repeater(中繼器)
   L1:  
   hub節點，皆於同一Collision Domain(碰撞網域)，易碰撞
   Repeater放大訊號
  
(2)Switch(交換器)    vs    Bridge(橋接器)
   L2:
   Bridge切割Collision Domain，提升頻寬
   Switch更細，每個Port都是獨立Collision Domain

(3)Router(路由器)    vs    Switch(交換器)
   L3:
   Router提供路由與轉送
   L3 Switch根據IP address轉送Packed

(4)Proxy(代理服務器)
   L7:
   可充當防火牆...等
```
# 7.簡述下列 address(位址)的意義與定義在OSI Model哪一層
(1)PORT address (2)IP address (3)MAC address
```
(1)L4: 電腦通訊的端點(endpoint)
(2)L3: 標識主機、網路定址
(3)L2: 一個NIC(網卡)一個MAC address
```
